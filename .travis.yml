os: osx
osx_image: xcode11.6
language: objective-c

jobs:
    include:
    - stage: Deployment
      env: Cocoapods
      script: pod lib lint
    - stage: Deployment
      env: Carthage
      script:
        - carthage build --no-skip-current
